// postcss.config.js
export default {
    plugins: {
      tailwindcss: {}, // Tailwind CSS
      autoprefixer: {}, // Autoprefixer (optional but recommended)
      "@tailwindcss/line-clamp": {}, // Line-clamp plugin
    },
  };